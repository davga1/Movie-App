import 'package:flutter/material.dart';

class LikedMovies extends StatelessWidget {
  const LikedMovies({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('nohting for now'),),);
  }
}
